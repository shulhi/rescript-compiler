let o1 = Option.getOrThrow(Some(3))
let o2 = Option.mapOr(Some(3), 0, x => x + 1)
let o3 = Option.getOr(None, 0)

let r1 = Result.getOrThrow(Ok(1))
let r2 = Result.mapOr(Ok(1), 0, x => x + 1)
let r3 = Result.getOr(Error("e"), 0)

let n1 = Null.getOrThrow(Null.make(3))
let n2 = Null.getOr(Null.null, 0)
let n3 = Null.mapOr(Null.make(3), 0, x => x)

let nb1 = Nullable.getOrThrow(Nullable.make(3))
let nb2 = Nullable.getOr(Nullable.null, 0)
let nb3 = Nullable.mapOr(Nullable.make(3), 0, x => x)

