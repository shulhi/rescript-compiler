open Mocha
open Test_utils

describe(__MODULE__, () => {
  test("gpr 1667 test", () => {
    let i = {
      let e =
        {
          let o = false

          (
            z =>
              {
                let m = ()
                n => 0
              }((q, y) => o)
          )(o)
        } == 0
      0
    }
    eq(__LOC__, i, 0)
  })
})
